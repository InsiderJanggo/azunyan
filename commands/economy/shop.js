const {MessageEmbed} = require("discord.js");

module.exports = {
    name: "shop",
    aliases: [""],
    description: "",
    usage: "",
    category: "economy",
    timeout: 5,
    run: async(bot, message, args) => {
       
    },
};