const {MessageEmbed} = require("discord.js");

module.exports = {
    name: "profile",
    aliases: ["pf", "$"],
    category: "economy",
    timeout: 3,
    description: "Show The User Bio",
    usage: "[mention]",
    run: async(bot, message, args) => {
        
    },
};